version: "3.9"

services:
  web:
    build: .
    container_name: xross-stars-tool
    ports:
      - "80:5000"
    environment:
      - DB_PATH=/app/data/app.db
    volumes:
      - ./data:/app/data
    restart: always